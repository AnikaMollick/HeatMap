import React from 'react'
import mapData from '../Heatmap/player-postions.json'



const data = () => {
    return (
        <div>
        {/* <h1>{JSON.stringify(mapData.map(x => x.top_view_position))} </h1> */}
        {/* <p> {JSON.stringify(mapData.map (y=> y.avg_color_hls))}</p>  */}
        </div>
    )
}

export default data

